#include <iostream>

using namespace std;
// declaration of variables
float abi1,hypoabi2,hypotenuseint;
int main()
{


    //input stage
    cout << "Hypotenuse calculator" << endl;
   cout <<" enter the adjacent"<<end ;
    cin>>abi1;
   cout<<"enter the opposite"<<end;
    cin>>abi2;
    hyotenuse=squrt((abi1*abi1)+(abi2*abi2));

cout<<"The hypotenuse is:"<<hypotenuse


    return 0;
}
